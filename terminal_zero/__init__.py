"""Terminal Zero — provenance-first research infrastructure.

Every number a user eventually sees is either quoted from a source document
or produced by executing a script that can be read and re-run. This package
is the ingest spine: fetch, parse, store, derive — with provenance on every row.
"""

__version__ = "0.0.1"
